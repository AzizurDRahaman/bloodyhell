#include<iostream>
using namespace std;

int main() {
    int n;
    cout<<"Enter the number: ";
    cin>> n;
if(num%2==0)
        cout<<"\n It will be an Even Number.";
    else
        cout<<"\n It will be an Odd Number.";
    return 0;
} 
